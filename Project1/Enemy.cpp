#include "Enemy.h"

void Enemy::attack() {
    std::cout << "Enemy attack" << std::endl;
}

// VisualElement (x, y, widht, height, hex)
// Character (x, y, width, geigh, hex) :: VisualElement(x, y, width, heigy, hex)
// Player(x, y) ::